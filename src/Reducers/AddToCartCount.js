export const addPopUpText = (state = "", action) => {
  switch (action.type) {
    case "ADDPOP":
      return state;
    case "REMOVEOPOUP":
      return state;

    default:
      return state;
  }
};
